import { Placeholder } from './Placeholder'

// Real print grid (scan + table + preview + PO) arrives in Phase 4.
export function PrintView() {
  return <Placeholder title="พิมพ์ป้ายราคา" subtitle="ตารางยิงรหัส + พรีวิว + PO — Phase 4" />
}
