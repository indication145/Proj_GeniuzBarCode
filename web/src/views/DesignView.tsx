import { Placeholder } from './Placeholder'

// Real editor (canvas + inspector + palette) arrives in Phase 4.
export function DesignView() {
  return <Placeholder title="สร้าง Template" subtitle="ตัวแก้ไขป้าย (canvas + inspector) — Phase 4" />
}
